# Expo HAS CHANGED

This project targets **Expo SDK 54** (pinned for Expo Go compatibility — Expo Go only runs one SDK version at a time). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — API shapes (e.g. `NativeTabs`, `expo-symbols`) differ from latest/other SDKs, and code written against newer docs will not typecheck or run here. When in doubt, check the actual installed type declarations in `node_modules` over the docs.
